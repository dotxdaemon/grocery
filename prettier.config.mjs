// ABOUTME: Defines Prettier formatting defaults for the grocery project.
// ABOUTME: Keeps code style consistent across TypeScript, JSX, and CSS files.
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  tabWidth: 2,
}

export default config
